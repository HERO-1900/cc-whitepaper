# V2 序章 Block 6 · OS 建立认知 · Brief

> 任务派发：双跑（Kimi 主跑 + MiniMax 主跑），独立产出后做交叉评审
> 块内职责：**首次完整建立 OS 类比框架**。Block 5 的"使命感" → Block 6 的"OS 框架就绪" → Block 7 的"信息密度顶点"。Block 6 是全书 OS 类比唯一首次"宣告 + 论证 + 打样"的窗口。
> 工时预算：5-6h（含 1 张图 0.3-A 43 工具目录 + 三件事对照面板 + 2 行 mapping row 打样 + 课程关联）
> Brief 撰写人：Opus
> 创建时间：2026-04-07 03:55
> 实例化模板：handoff/v2-prologue-brief-template-2026-04-06.md
> 素材原料包：handoff/v2-prologue-block6-prep-2026-04-07.md（含 12 行 OS 对照表全清单 + 8 段通俗理解清单 + 7 OQ）
> Opus 7 OQ 决断：见 §0.2

---

## 0. 给生成 Sub-Agent 的硬约束（最优先读）

**MUST**:
- 输出唯一一份完整 HTML 文件，无外部依赖
- token 引用：仅 `var(--cc-*)`，字体仅 `var(--cc-font-sans)` + `var(--cc-font-mono)`（FM-12）
- 数字精度：所有数字逐位精确，禁止约等于（FM-09）
- dark 主题为基线 + `[data-theme="light"]` 浅色覆盖
- 不调用任何 JavaScript 框架或 CDN
- **三件事对照面板必出**：OS 三件事（管理硬件资源 / 调度应用程序 / 执行安全策略）↔ CC 三件事（管理 token 预算和 API 配额 / 调度 AI 智能体实例 / 执行权限规则和沙箱限制）
- **2 行 mapping row 打样必出**：内核 ↔ QueryEngine + 系统调用 ↔ 工具调用，每行配 💡 通俗理解块（快递分拣 + 持证上岗）
- **0.3-A 43 工具目录图必出**（VIS-0-010 / `test-viz/production/html/VIS-0-010_43个工具完整目录.html`），6 分类：File / Search / Execute / Agent / Web / Other
- **📚 课程关联块必出**（OQ6 决断 A，保留 OS + Software Engineering 课程锚点）
- **Q1「这不是为了修辞好看——Claude Code 的架构*真的*像一个操作系统」**必须逐字 + 大字号 pull-quote，"真的"两字加重强调
- **块尾必须为 Block 7a「安全防线」预热**（OQ5 决断 A）

**MUST NOT**:
- 严禁 `iframe` / `Math.random` / 凑数式数据补全
- 严禁拿 LLM 自己虚构的 OS 概念 / 工具 / 数据
- **严禁出现 OS 对照表的 10 行后续映射**（进程调度 / 文件系统 / 安全模型 / 设备驱动 / Shell / /etc/ / IPC / Boot / 内核模块 / 包管理器）—— 这些是 Block 7 的弹药
- **严禁出现 6 段后续通俗理解**（外卖骑手 / 小区门禁 / USB 转接口 / 穿衣层次 / 早上起床 / 快递柜短信）
- 严禁混入 Block 4 hero 数字 (1,884 / 476,875 / 40 / 98 / 一个为 AI 智能体设计的操作系统)
- 严禁混入 Block 5 数字与短语 (83 章 / 360,583 / 122 图表 / 185 / 这不是使用指南 / 这是工程解剖 / 我们不猜测)
- 严禁混入 Block 7 的 7 张图任何细节 (0.3-B/C/D/E/F/G/H)
- 严禁混入 Block 7 的细节数字 (7 任务类型 / 27 Hook 事件 / 6 权限模式 / 8 MCP 传输 / 9 配置层 / 5 扩展机制)
- 严禁混入 Block 8 的三类阅读路径 + Block 9 的 5 比喻家族 / 源码入口 / 让我们开始
- 严禁混入 Block 1-3 任何元素

### 0.1 数字纪律红线

| 数字 | Block 6 是否可用 | 用途 |
|------|----------------|------|
| **3**（OS 三件事 / CC 三件事） | ✅ 主场 | 三件事对照面板 |
| **6**（0.3-A 工具目录 6 分类） | ✅ 主场 | 0.3-A 副标 |
| **43**（0.3-A 工具数完整目录） | ✅ 主场 | 0.3-A 标题数字 |
| 40（Block 4 hero 工具数）| ❌ 严禁 | Block 4 已用 |
| 38（Block 9 src/tools/ 工具数） | ❌ 严禁 | Block 9 已用 |
| Block 4/5 hero 数字 | ❌ 严禁 | — |
| Block 7 细节数字（7 任务/27 Hook 等） | ❌ 严禁 | — |

### 0.2 Opus 7 OQ 决断（必须严格执行）

| OQ | 议题 | Opus 决断 | 违反后果 |
|----|------|---------|---------|
| **OQ1** | 0.3-A 是否在 Block 6 | **A 在 Block 6**（按拆块方案 L158）。Block 6 范围 = L91-110，含 0.3-A 图表 | 把 0.3-A 移到 Block 7 = -3 分 |
| **OQ2** | 工具数三口径 (40/38/43) 如何统一 | **A 守 L110「43」**。**只用 43**，**禁止**出现 40/38。0.3-A 标题数字 = 43。Footnote 不必，因为 Block 6 不并列 Block 4/9 的口径 | 出现 40 或 38 = -3 分 |
| **OQ3** | 进程调度器行（L112）归属 | **A 严格 2 行**。Block 6 只用「内核 + 系统调用」两行，调度器留 Block 7 | 出现"进程调度器 ↔ Agent/Task" mapping row = -3 分 |
| **OQ4** | 视觉主体 | **A 三件事面板 + 2 行 mapping row + 0.3-A 图表三段式**（拆块方案 L163-164 字面） | 缺任一段 = -2 分 |
| **OQ5** | Block 7a 预热 | **A 用 Block 7 prep 建议接力句**：「**OS 类比表已经把概念坐标系建立起来。但有些子系统重要到一句类比说不清——它们需要图。我们从最关键的'安全'开始。**」 + 「**安全才是 AI 智能体的第一道门——而它本身就是一个矩阵。**」 | 缺 7a 预热 = -3 分 |
| **OQ6** | 📚 课程关联块 | **A 保留**作 Block 6 内的"📚 教学边栏"。完整保留 L97 原文（Operating Systems / Silberschatz / Software Engineering / Reverse Engineering / Architecture Analysis 全部关键词） | 删除 = -3 分 |
| **OQ7** | 派单 | **B 升级为双跑**（Kimi + MiniMax 各自独立产出，与其它块统一） | — |

---

## 1. 块在 11 块叙事弧中的位置

```
块1 → 块2 → 块3 → 块4 → 块5 → [块6] → 块7a → 块7b → 块7c → 块8 → 块9
hero  反差  生态  数字  使命   OS建立  安全  接口  内部  分流  收尾
```

- **本块**：第 6 拍
- **节奏角色**：☆ **呼吸位 / 概念锚定 / 第二次推进的第二拍**。能量值必须**低于** Block 4（数字震撼）和 Block 7（信息密度顶点），是"认知准备 → 全力深潜"的最后一步缓冲。
- **上一块（Block 5 使命陈述）交接情绪**：刚被「这不是使用指南 / 这是工程解剖」对照卡 + 三类读者画像 + 方法论 badge 安抚为"使命感"——"好，这本书要帮我看清这一切"。
- **我要交给下一块（Block 7a 安全防线）的情绪**：把"使命感"递为"OS 框架就绪"——"现在我有了一把尺子（OS 类比），可以测量 Claude Code 的每个子系统了。从最关键的'安全'开始。"

## 2. 内容承载

### 2.1 原文（来自 `book/part0_序章/00_序章.md` 第 91-110 行）

```markdown
## 为什么用"操作系统"来理解 Claude Code

这不是为了修辞好看——Claude Code 的架构**真的**像一个操作系统。

当你的电脑开机时，操作系统做三件事：管理硬件资源、调度应用程序、执行安全策略。Claude Code 做的事几乎一样，只不过"硬件资源"变成了 token 预算和 API 配额，"应用程序"变成了 AI 智能体实例，"安全策略"变成了权限规则和沙箱限制。

> 📚 **课程关联**：本书的分析框架大量借鉴**操作系统**（Operating Systems）课程的核心概念。如果你正在或即将学习这门课，Claude Code 是一个绝佳的"现代操作系统"案例研究——它把教科书中的抽象概念（进程调度、内存管理、安全模型、文件系统）投射到了 AI 应用的全新领域。同时，本书涉及的工程分析方法对应**软件工程**（Software Engineering）课程中的逆向工程（Reverse Engineering）和架构分析（Architecture Analysis）实践。

这个类比能帮你建立直觉。下面这张表是全书的"翻译对照表"——每当你在后续章节中遇到一个 Claude Code 的概念，你可以在这里找到它在操作系统世界中的对应物：

| 操作系统概念 | Claude Code 对应物 | 为什么这个类比成立 |
|---|---|---|
| **内核 (Kernel)** | QueryEngine + queryLoop（核心循环——接收请求、调用工具、返回结果） | 所有请求都经过它调度——调用 API、分发工具、管理上下文，就像内核管理 CPU 时间片和内存分配 |

> 💡 **通俗理解**：QueryEngine 就像**快递分拣流水线**——收件（接收用户输入）→ 分拣（判断需要什么工具）→ 装车（调用 API）→ 送达（执行工具）→ 签收（返回结果）→ 等下一单（继续循环）。

| **系统调用 (Syscall)** | 工具调用 (Tool invocation) | AI 不能直接读写文件——它必须通过工具系统发出"系统调用"，就像用户态程序必须通过 syscall 访问硬件 |

> 💡 **通俗理解**：工具就像**员工的工作技能证书**——读文件证书、写代码证书、搜索证书，每个技能需要考核通过（权限检查）才能用。Claude 不是万能的，它需要"持证上岗"。

> **[图表预留 0.3-A]**：43 个内置工具完整目录——按 6 个类别（File / Search / Execute / Agent / Web / Other）分组，标注每个工具的权限要求、输入 schema 与适用场景。
```

### 2.2 必保留事实清单

**A 类 — 三件事对照面板（必出，OS 类比成立的核心论据）**

| OS 三件事 | CC 对应物 | 来源 |
|---|---|---|
| **管理硬件资源** | **管理 token 预算和 API 配额** | L95 |
| **调度应用程序** | **调度 AI 智能体实例** | L95 |
| **执行安全策略** | **执行权限规则和沙箱限制** | L95 |

**B 类 — 2 行 OS 对照表 mapping row 打样（必出，禁止超出这 2 行）**

| OS 概念 | CC 对应物 | 类比成立理由 | 来源 |
|---|---|---|---|
| **内核 (Kernel)** | QueryEngine + queryLoop（核心循环——接收请求、调用工具、返回结果） | 所有请求都经过它调度——调用 API、分发工具、管理上下文，就像内核管理 CPU 时间片和内存分配 | L103 |
| **系统调用 (Syscall)** | 工具调用 (Tool invocation) | AI 不能直接读写文件——它必须通过工具系统发出"系统调用"，就像用户态程序必须通过 syscall 访问硬件 | L106 |

**C 类 — 2 段「💡 通俗理解」（必出，每个 mapping row 各配一段，逐字保留）**

| 通俗类比 | OS 概念 | 内容（逐字） | 来源 |
|---|---|---|---|
| **快递分拣流水线** | 内核 / QueryEngine | 收件（接收用户输入）→ 分拣（判断需要什么工具）→ 装车（调用 API）→ 送达（执行工具）→ 签收（返回结果）→ 等下一单（继续循环） | L105 |
| **员工的工作技能证书 / 持证上岗** | 系统调用 / 工具 | 读文件证书、写代码证书、搜索证书，每个技能需要考核通过（权限检查）才能用。Claude 不是万能的，它需要"持证上岗" | L108 |

**D 类 — 必出现的金句**

| # | 金句 | 来源 | 用途 |
|---|---|---|---|
| Q1 | **「这不是为了修辞好看——Claude Code 的架构*真的*像一个操作系统。」** | L93 | 块首"反修辞宣言"大字号 pull quote。**最强锚点**："*真的*"两字斜体加重 |
| Q2 | **「这个类比能帮你建立直觉。下面这张表是全书的'翻译对照表'。」** | L99 | OS 表前导句，2 行 mapping row 之上 |
| Q3 | 「'硬件资源'变成了 token 预算和 API 配额，'应用程序'变成了 AI 智能体实例，'安全策略'变成了权限规则和沙箱限制。」 | L95 | 三件事对照面板的右列说明 |
| Q4 | 「AI 不能直接读写文件——它必须通过工具系统发出'系统调用'，就像用户态程序必须通过 syscall 访问硬件。」 | L106 | 0.3-A 图表上方的导引大字 |

**E 类 — 📚 课程关联块（必出，OQ6 决断 A）**

L97 整段必须保留，含以下关键词：
- **操作系统**（Operating Systems）课程
- "现代操作系统"案例研究
- 教科书中的抽象概念：**进程调度 / 内存管理 / 安全模型 / 文件系统**
- **软件工程**（Software Engineering）课程
- **逆向工程**（Reverse Engineering）
- **架构分析**（Architecture Analysis）

视觉处理建议：📚 emoji + 加粗"课程关联"标签 + 浅色背景边栏

**F 类 — 0.3-A 43 工具目录（必出图表）**

| 字段 | 值 | 来源 |
|---|---|---|
| 图表 ID | VIS-0-010 / 0.3-A | L110 + vis-soul |
| 图名 | 43 个内置工具完整目录 | L110 |
| 分类 | 6 个：File / Search / Execute / Agent / Web / Other | L110 |
| 必保留特征 | 43 工具穷举 / 6 分类颜色 / 属性标签 (只读/可写/破坏性/支持并发/需确认) / 分类筛选 + 搜索 | vis-soul L356 |
| HTML 现状 | `test-viz/production/html/VIS-0-010_43个工具完整目录.html` (879 行 / 8/10 评分) | vis-soul |

### 2.3 严禁出现的内容（10 行 OS 表 + 6 段通俗理解 + 其它块边界）

**OS 对照表 10 行禁区**（这 10 行是 Block 7 弹药）：
- 进程调度器 ↔ Agent/Task 系统
- 文件系统 ↔ File History + JSONL
- 安全模型 ↔ 权限系统 + 沙箱
- 设备驱动 ↔ MCP 服务器
- Shell ↔ REPL 终端
- /etc/ 配置 ↔ 设置系统
- 进程间通信 ↔ Scratchpad
- 开机引导 ↔ main.tsx → init.ts
- 内核模块 ↔ Hooks 系统
- 包管理器 ↔ 插件系统 + Skills

**6 段通俗理解禁区**：外卖调度中心 / 小区门禁 / MacBook USB 转接口 / 穿衣服层次 / 早上起床流程 / 快递柜短信通知

**其它禁区**：Block 1-5 / Block 7-9 任何元素（详见 §0 MUST NOT）

## 3. 视觉语言

### 3.1 推荐视觉组件（≥4 类，呼吸位克制）

| 组件 | 用途 | 优先级 |
|---|---|---|
| **prologue-marginal-label** + **hero-footer**（块号 06 + 块名 + sub-title） | 全书 spec 复制 | **P0 必出** |
| **block-title** "为什么用'操作系统'来理解 Claude Code" | H2 标题 | **P0 必出** |
| **q1-rejection-rhetoric** | Q1 大字号 pull-quote「这不是为了修辞好看……*真的*像一个操作系统」，"真的"斜体加重 | **P0 必出** |
| **three-things-panel** | 三件事对照面板：OS 三件事 ↔ CC 三件事，左右双列 | **P0 必出** |
| **course-tag-callout (📚)** | L97 课程关联块，📚 emoji + 浅色边栏 | **P0 必出** |
| **q2-table-header** | Q2「全书的翻译对照表」前导句 | P0 |
| **mapping-row x 2** | 2 行 OS ↔ CC mapping，每行 3 列（OS 概念 / CC 对应 / 类比理由） | **P0 必出** |
| **insight-block 💡 x 2** | 2 段通俗理解，每段紧邻对应 mapping row 之下 | **P0 必出** |
| **vis-0-010-embed** | 0.3-A 43 工具目录图（embed 或链接，不重新生成） | **P0 必出** |
| **block-7a-bridge** | 块尾过渡到 Block 7a 的钩子句 | **P0 必出** |

### 3.2 节奏建议（必须严格按这个顺序铺陈）

1. **顶部 marginal label + hero footer**："序章 · Block 06 · OS 建立认知"
2. **块标题**：「## 为什么用"操作系统"来理解 Claude Code」
3. **Q1 大字号反修辞宣言**：「这不是为了修辞好看——Claude Code 的架构 *真的* 像一个操作系统。」
4. **三件事对照面板**：
   - 引导句（部分 L95）：「当你的电脑开机时，操作系统做三件事……」
   - 左右双列对照：OS 三件事 ↔ CC 三件事
   - Q3 说明：「'硬件资源'变成了 token 预算……」
5. **📚 课程关联块（教学边栏）**：L97 整段，📚 + 浅色背景边栏
6. **Q2 对照表前导**：「这个类比能帮你建立直觉。下面这张表是全书的'翻译对照表'。」
7. **OS 对照表 2 行打样**：
   - 表头：操作系统概念 / Claude Code 对应物 / 为什么这个类比成立
   - 第 1 行：内核 (Kernel) ↔ QueryEngine + queryLoop
   - 💡 通俗理解：快递分拣流水线
   - 第 2 行：系统调用 (Syscall) ↔ 工具调用 (Tool invocation)
   - 💡 通俗理解：员工的工作技能证书 / 持证上岗
8. **Q4 0.3-A 图表前导大字**：「AI 不能直接读写文件——它必须通过工具系统发出'系统调用'……」
9. **0.3-A 43 工具目录图**（VIS-0-010 嵌入或链接）
10. **块尾 Block 7a 预热（OQ5）**：
    - 「**OS 类比表已经把概念坐标系建立起来。但有些子系统重要到一句类比说不清——它们需要图。我们从最关键的'安全'开始。**」
    - 「**安全才是 AI 智能体的第一道门——而它本身就是一个矩阵。**」

### 3.3 字体/留白/对比度建议

- 块标题：`var(--cc-font-size-4xl)` ~ `5xl`，bold（呼吸位降一档）
- Q1 反修辞宣言：`var(--cc-font-size-3xl)` ~ `4xl`，"*真的*"加斜体或下划线 emphasis
- 三件事对照面板：左右双列 grid，每列文字 `var(--cc-font-size-lg)`
- 课程关联块：背景 `var(--cc-color-surface-secondary)`，📚 emoji + bold 标签，正文 `var(--cc-font-size-base)`
- 2 行 mapping row：表格或 grid 三列，OS 概念列字号 `xl`，CC 对应列 `lg`，类比理由列 `base`
- 💡 通俗理解：浅色背景 + 💡 emoji + bold "通俗理解" 标签 + 比喻名加粗
- 0.3-A 图表：全宽嵌入或居中链接卡片
- 块尾过渡句：字号 `2xl` ~ `3xl`，独立成段
- block padding：`var(--cc-space-12)` ~ `space-16` 上下
- 整体留白：呼吸位语境，比 Block 4 显著大，比 Block 7 略小

## 4. 双源理解（本块 1 张图表 0.3-A）

### 4.1 VIS-0-010 / 0.3-A 43 工具完整目录

**Brief 期望**（vis-soul L339-359）：
- 43 个内置工具分 6 类：File 约 12 / Search 约 6 / Execute 约 4 / Agent 约 3 / Web 约 2 / Other 若干
- 每张工具卡：工具名 + 一句话描述 + 属性标签（只读 / 可写 / 破坏性 / 支持并发 / 需确认）
- 顶部分类筛选按钮 + 搜索框
- 每类标题显示工具数量

**HTML 现状**（评分 8/10，879 行）：
- 命中 Brief：卡片网格 CSS Grid auto-fill / 6 分类颜色 / 属性标签体系完整
- 偏离 Brief：（vis-soul 未列大缺陷，整体高质量）

**Block 6 改造方向**：
- **复用 + 微调**。结构沿用 V1，确保与 Block 6 的呼吸位节奏不冲突
- **必须保留**：43 工具穷举（不抽样）、6 分类颜色（文件蓝/搜索绿/执行橙/Agent 紫/Web 青/Other 灰）、属性标签 pill 形式、分类筛选 + 搜索
- **节奏约束**：动效密度低于 Block 7 的 7 张图

## 5. 评审 Checklist（评审 Sub-Agent 用，满分 75）

| # | 检查项 | 评分依据 | 分值 |
|---|---|---|---|
| 1 | Q1「这不是为了修辞好看……*真的*像一个操作系统」逐字 + 大字号 | 缺 = 0 分 | 5 |
| 2 | 三件事对照面板：OS 3 件 ↔ CC 3 件，左右双列 | grep `管理硬件资源` `调度应用程序` `执行安全策略` `token 预算` `AI 智能体实例` `权限规则` | 5 |
| 3 | 内核 (Kernel) ↔ QueryEngine mapping row | grep 完整 | 5 |
| 4 | 系统调用 (Syscall) ↔ 工具调用 mapping row | grep 完整 | 5 |
| 5 | 💡 通俗理解 1：快递分拣流水线 | grep `快递分拣` `收件` `分拣` `装车` `送达` `签收` | 5 |
| 6 | 💡 通俗理解 2：员工的工作技能证书 / 持证上岗 | grep `工作技能证书` `持证上岗` `读文件证书` `写代码证书` | 5 |
| 7 | 📚 课程关联块完整：操作系统 + 软件工程 + 逆向工程 + 架构分析 | grep 4 个关键词 | 5 |
| 8 | 0.3-A 43 工具目录图嵌入或链接（VIS-0-010） | grep `43` `内置工具` `File` `Search` `Execute` `Agent` `Web` `Other` | 5 |
| 9 | 块尾 Block 7a 过渡句 | grep `安全才是 AI 智能体的第一道门\|安全.*第一道门\|从最关键的'安全'开始` | 5 |
| 10 | OQ3 严格 2 行 OS 表（不出现进程调度器、文件系统等其它 10 行） | grep `进程调度器 ↔\|文件系统 ↔\|MCP 服务器\|Shell\|/etc/\|内核模块`（OS 表行模式）必须 0 | 5 |
| 11 | 6 段后续通俗理解禁区 grep 全 0：`外卖.*骑手` `小区门禁` `USB 转接口` `穿衣服层次` `早上起床` `快递柜.*短信` | 任 1 命中 = -3 分 | 5 |
| 12 | 工具数仅用 43，禁用 40 / 38 | grep `40 个 AI 工具\|40 个工具.*[^43]\|38 个内置工具` 必须 0 | 5 |
| 13 | 严禁混入 Block 4 hero（1,884 / 476,875 / 一个为 AI 智能体设计的操作系统）| grep 全 0 | 5 |
| 14 | 严禁混入 Block 5 元数据（83 章 / 360,583 / 122 / 185 / 这不是使用指南 / 这是工程解剖）| grep 全 0 | 5 |
| 15 | token 纪律：var(--cc-*) ≥ 80 / 字体仅 sans+mono | grep | 5 |

**满分 75 分**，**≥60 分**才允许进入下一阶段融合。

## 6. 输出契约

- 文件名：`v2-demos/block6/prologue-block6-<kimi|minimax>-<timestamp>.html`
- 文件大小预算：40-80 KB（含 0.3-A 图表）
- 行数预算：700-1,200 行
- 所有数字 / 金句 / OS 概念 / 通俗理解关键词必须能 grep 到

## 7. 特殊提醒

- **本块灵魂 = 三件事对照面板 + Q1 反修辞宣言 + 2 段通俗理解**。如果做成"OS 教科书风格"列表，整个块就废了——这是"建立框架" 而不是"灌输知识"。
- **严格守 2 行**（OQ3）：哪怕看起来"再加一行更丰满"也不能加。Block 7 需要 10 行的弹药。
- **0.3-A 工具数 = 43**（OQ2）：不出现 40 / 38。Block 6 是 0.3-A 的主场，工具数口径以这里为准。
- **课程关联块是 CS 学生救命稻草**（OQ6）：alex-wang R2「目标读者召唤」要求 Block 6 / Block 8 协同覆盖学生读者。
- **块尾过渡句不是 Block 7a 的预告，是 Block 6 的告别**。两段（"OS 类比表已经……" + "安全才是……"）合成一个收束。
- **OS 比喻在这里是主菜**：Block 6 是项目级写作风格红线"OS 比喻独大"的部分豁免位——但仍必须保留 2 段日常生活通俗理解（快递分拣 + 持证上岗）平衡。
- **不要预支 Block 7 的 10 行 OS 表**：哪怕只是隐晦提及"调度器"或"安全模型"也要小心。

## 8. 与其他子 Agent 的协作约定

- 双跑模式：Kimi 主跑 + MiniMax 主跑独立产出
- 评审 sub-agent 用 §5 checklist 各打一份分
- 最终融合由 Opus 决定

---

*Brief by Opus on 2026-04-07 03:55 — 严格基于 Block 6 prep + Sonnet 抽取 7 OQ + Opus 决断 + 序章 L91-110 逐字，零虚构。Block 6 是全书 OS 类比框架唯一首次完整建立位置。*
