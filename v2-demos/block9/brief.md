# V2 序章 Block 9 · 收尾合一（比喻 + 声明 + 源码入口） · Brief

> 任务派发：双跑（Kimi 主跑 + MiniMax 主跑），独立产出后做交叉评审
> 块内职责：V2 序章 11 块叙事弧的**最后一块**——收尾呼吸位 / 全篇告别 / "让我们开始"的导火索。包含 3 个语义子单元：关于比喻 / 一个声明 / 源码入口 + 1 个 CTA 尾巴。
> 工时预算：3-4h（无数字震撼，无大图表，纯叙事卡片 + 等宽源码块 + 巨型 CTA）
> Brief 撰写人：Opus
> 创建时间：2026-04-07 02:45
> 实例化模板：handoff/v2-prologue-brief-template-2026-04-06.md
> 素材原料包：handoff/v2-prologue-block9-prep-2026-04-07.md（含 5 比喻家族 / 7 硬数字 / 5 源码入口 / 7 开放问题 + 9 项专属 checklist）
> Opus 7 OQ 决断（必须严格执行）：见 §0.2 OQ 决断纪律表

---

## 0. 给生成 Sub-Agent 的硬约束（最优先读）

**MUST**:
- 输出唯一一份完整 HTML 文件（含 `<!DOCTYPE html>`、`<head>`、`<style>`、`<body>`），无外部依赖
- token 引用：仅使用 `var(--cc-*)`，不得在本块内重新定义任何 `--cc-*` 变量
- 字体：`var(--cc-font-sans)` 正文 + `var(--cc-font-mono)` 数字/代码（**源码入口 5 行必须用 mono**），不得出现任何系统字体名（违反 SOP FM-12）
- 数字精度：本 Brief §2.2 列出的所有"硬数字"必须**逐位精确**，严禁约等于（不得出现 `~|大约|approx|K行|万行` 字样，违反 SOP FM-09）
- **唯一允许的 "约" 字**：`约 1,400 行`（这是序章原文 L235 的原文，不得删"约"字也不得另增其它"约"字）
- 配色：dark 主题为基线，浅色由 `[data-theme="light"]` 覆盖，两套都要可用
- 不得调用任何 JavaScript 框架或 CDN，纯 CSS + 极少量必要 vanilla JS（仅主题切换）
- 5 个比喻家族**全部出场，零删减**，逐字保留括号里的领域标签
- L197 / L201 两条金句（"好的比喻不是降低精度，是提供另一个入口。" + "比喻先行，精确随后。"）必须**逐字出现**且做 pull-quote 视觉强调
- L223 "让我们开始。" 必须**逐字出现**作为"一个声明"段落的收束句
- L241 CTA "翻到 Part 1 第一章" + "这不是聊天机器人" 必须**逐字出现**（可重新设计成巨型按钮，但 6 个字 + 章名一字不能改）

**MUST NOT**:
- 严禁使用 `iframe`
- 严禁 `Math.random` / `while(...length<N)` 这类"凑数式数据补全"（违反 SOP FM-11）
- 严禁拿 LLM 自己虚构的数字 / 比喻 / 源码路径
- **严禁补全图表预留 0.5-A 的"25 个核心概念"具体名字**——序章原文只给了 5 个分组名称，没给 25 个概念。任何 sub-agent **不得自行虚构 25 个概念名**，只能展示 5 个分组标签。违者评审 0 分。
- 严禁删 5 比喻家族中的任何一家
- 严禁把"38 个内置工具"改成 40 / 43 / 35 等其它数字（即使你认为别的更准）
- 严禁删除 L239 的 `---` 分隔线（视觉上必须保留为水平线）
- 严禁出现"生成式占位文本"（lorem ipsum / `// TODO` / `// 此处应有...`）
- **严禁混入 Block 4 的 hero 数字**（1,884 / 476,875 / 40 / 98 / 185 / 7 等）—— Block 9 是收尾呼吸位，节奏不能再"震撼"
- 严禁混入 Block 3 的社区数字（9,000+ / 121K / 1.1k 等）
- **严禁纳入 F9.1 的 11 类 Prompt 入口表 或 F9.2 的 12,000 字 init.ts 钩子**——见 §0.2 OQ4 / OQ5 决断
- 严禁为 5 比喻家族凭空虚构章节锚点（章节号未知就留空 / 写"详见 Part X"）

### 0.2 OQ 决断纪律表（Opus 7 拍板，违者评审 0 分）

| OQ | 议题 | Opus 决断 | 违反后果 |
|----|------|---------|---------|
| **OQ1** | L234 "38" vs 全序章其他位置 "40 / 43" | **保留 38**，加小字脚注："此处统计 src/tools/ 目录下的工具实现文件，与 0.3-A 的 43 个对外可调用工具在口径上不同" | 改 38 = 0 分 |
| **OQ2** | 0.5-A 速查表的 25 个概念是否补全 | **不补全**。只展示 5 个分组（循环与执行 / 工具与 Agent / 安全与权限 / 上下文与 Token / 扩展与配置）作为 5 张分类标签卡。**不许虚构 25 个概念名** | 凭空补 = 0 分 |
| **OQ3** | 5 比喻家族的章节锚点 | **留空 / 写"详见后续章节"**。不准虚构具体章号（如 "Part 4 第 11 章"），因为序章原文未声明 | 凭空补章号 = -3 分 |
| **OQ4** | 12,000 字 init.ts 钩子是否纳入 | **不纳入**。Block 9 不引入序章原文以外的数字 | 出现 12,000 / NEW_INIT_PROMPT = -2 分 |
| **OQ5** | 11 类 Prompt 入口（F9.1）是否纳入 | **方案 α 保守**：只展示 L232-236 原文 5 行 src/* 入口。**不展示 11 类细分** | 出现 11 类表 = -3 分 |
| **OQ6** | L239 `---` 分隔线 | **保留**为视觉水平线（CSS `border-top` 或 `<hr>`），位于"一个声明"块尾 / "源码入口"块头之间 | 删除 = -1 分 |
| **OQ7** | L231-237 源码入口块的视觉对齐 | **等宽 `<pre>` 字体保留原文视觉对齐**（用 mono 字体 + `white-space: pre`）。允许做颜色高亮，但不得用 CSS grid 重列 | 用 grid 重列 = -2 分 |

---

## 1. 块在 11 块叙事弧中的位置

```
块1 → 块2 → 块3 → 块4 → 块5 → 块6 → 块7a → 块7b → 块7c → 块8 → [块9]
hero  反差  生态  数字  使命  OS建立 安全  接口  内部  分流   收尾
```

- **本块**：第 9 拍，最后一拍
- **节奏角色**：**收尾呼吸位 / 全篇告别 / 让我们开始**。Pentagram 式"震撼 → 缓和 → 告别 → 启程"的告别+启程位。
- **上一块（Block 8 分流）交接情绪**：刚被"按读者身份分流"（开发者 / 研究者 / 创业者）四类阅读路径建议引导，开始有"对了我属于哪一类，我应该怎么读"的代入感
- **我要交给读者的最终情绪**：被 5 比喻家族的世界观点亮 → 被 Anthropic 致谢的真诚感染 → 看到 5 行精炼源码入口想"哎 真的能直接读？" → 被巨型 CTA "翻到 Part 1 第一章" 推一把，**愿意翻页继续读**

## 2. 内容承载

### 2.1 原文（来自 `book/part0_序章/00_序章.md` 第 195-241 行，含 3 子单元 + 1 CTA 尾巴）

**子单元 A：关于比喻（L195-213）**

```markdown
## 关于比喻

这本书会大量使用比喻和类比。这不是为了"通俗化"——是因为 Claude Code 涉及的很多概念（投机执行、提示词缓存边界、权限状态机、Token 预算）本身就很抽象。**好的比喻不是降低精度，是提供另一个入口。**

我们遵循一个原则：

> **比喻先行，精确随后。** 先用你已经懂的东西（操作系统、机场安检、城市财政）建立直觉，再用源代码和行号确认这个直觉是否准确。如果比喻和现实有出入，我们会明确指出哪里类比不成立。

你会在书中看到几个反复出现的比喻家族：

- **操作系统**（全局框架）：内核、系统调用、文件系统、进程调度……
- **机场安检**（安全模型）：安检通道、PreCheck、登机牌、海关……
- **城市财政**（Token 经济学）：预算、税收、基础设施复用……
- **三明治组装线**（提示词构建）：食材顺序、配方固定部分、个性化配料……
- **游戏存档**（文件历史）：自动存档、存档点、读档回滚……

每个比喻第一次出现时都会完整展开，后续引用时只用简称。

> **[图表预留 0.5-A]**：Claude Code 概念速查表——25 个核心概念分 5 组（循环与执行 / 工具与 Agent / 安全与权限 / 上下文与 Token / 扩展与配置），阅读后续章节时可随时回这里查对。
```

**子单元 B：一个声明（L217-223）**

```markdown
## 一个声明

本书的分析基于 Claude Code 2.1.88 版本的泄露源代码。我们对 Anthropic 的工程团队怀有真诚的敬意——这是一个设计精良的系统，很多决策在深入分析后让人由衷佩服。我们的批评同样真诚：指出设计取舍和潜在问题不是贬低，而是工程分析应有的态度。

代码会演进，版本会更新。书中的行号和具体实现可能在你阅读时已经改变。但设计理念和架构决策的生命力远比具体代码长——这才是本书试图传递的核心价值。

让我们开始。
```

**子单元 C：快速导航源码入口（L227-237）**

```markdown
## 快速导航：源码入口

本书分析的核心源码入口：

```
src/main.tsx          — 启动入口（4,684 行的巨型文件）
src/services/claude.ts — 查询循环核心（query 函数）
src/tools/            — 38 个内置工具的实现
src/utils/permissions/ — 权限系统（约 1,400 行）
src/memdir/           — 记忆系统
```
```

**CTA 尾巴（L241，前面紧接 L239 的 `---` 分隔线）**

```markdown
---

*→ 翻到 Part 1 第一章：[这不是聊天机器人](../part1_认识这个系统/01_这不是聊天机器人.md)*
```

### 2.2 必保留事实清单（不得删，不得简化，逐位精确）

**A 类 — 5 比喻家族（不得删任何一家，括号领域标签必须逐字保留）**

| # | 比喻家族 | 领域标签 | 子概念（逐字） | 来源 |
|---|---|---|---|---|
| M1 | **操作系统** | （全局框架） | 内核、系统调用、文件系统、进程调度…… | L205 |
| M2 | **机场安检** | （安全模型） | 安检通道、PreCheck、登机牌、海关…… | L206 |
| M3 | **城市财政** | （Token 经济学） | 预算、税收、基础设施复用…… | L207 |
| M4 | **三明治组装线** | （提示词构建） | 食材顺序、配方固定部分、个性化配料…… | L208 |
| M5 | **游戏存档** | （文件历史） | 自动存档、存档点、读档回滚…… | L209 |

**B 类 — 必出现的金句（pull-quote 必出 2 条）**

| # | 金句 | 来源 | 视觉处理 |
|---|------|------|---|
| Q1 | **「好的比喻不是降低精度，是提供另一个入口。」** | L197 加粗 | pull-quote 大字号，建议 `var(--cc-font-size-3xl)` |
| Q2 | **「比喻先行，精确随后。」** | L201 加粗 | 方法论口号，建议 `var(--cc-font-size-2xl)` 起跳，配合下方说明 "先用你已经懂的东西……再用源代码和行号确认……" 整段 blockquote |

**C 类 — 必出现的 7 个硬数字（带 file:line 来源）**

| # | 数字 | 含义 | 来源 | 备注 |
|---|------|------|---|---|
| D1 | **5** | 5 个比喻家族 | L205-209 | 用 `<ul>` 5 项或 5 卡片 |
| D2 | **25** | 0.5-A 速查表的核心概念数 | L213 图表预留 | 只在标签里写"25 个核心概念分 5 组"，**不展开 25 个名字** |
| D3 | **5** | 0.5-A 速查表的分组数 | L213 图表预留 | 5 个分组标签 |
| D4 | **2.1.88** | Claude Code 版本号 | L219 | 不得写成 2.1.87 / 2.2 / "最新版" |
| D5 | **4,684** | src/main.tsx 行数 | L232 | 带千分位逗号 |
| D6 | **38** | src/tools/ 内置工具数 | L234 | **见 §0.2 OQ1 — 必须 38 + 小字脚注** |
| D7 | **约 1,400** | src/utils/permissions/ 行数 | L235 | **唯一允许带"约"的数字** |

**D 类 — 必出现的 5 行源码入口（逐字保留）**

| # | 路径（逐字） | 注释（逐字） | 来源 |
|---|---|---|---|
| P1 | `src/main.tsx` | — 启动入口（4,684 行的巨型文件） | L232 |
| P2 | `src/services/claude.ts` | — 查询循环核心（query 函数） | L233 |
| P3 | `src/tools/` | — 38 个内置工具的实现 | L234 |
| P4 | `src/utils/permissions/` | — 权限系统（约 1,400 行） | L235 |
| P5 | `src/memdir/` | — 记忆系统 | L236 |

**E 类 — 0.5-A 速查表的 5 个分组标签（必出现，不展开）**

| # | 分组名 | 来源 |
|---|------|---|
| G1 | 循环与执行 | L213 |
| G2 | 工具与 Agent | L213 |
| G3 | 安全与权限 | L213 |
| G4 | 上下文与 Token | L213 |
| G5 | 扩展与配置 | L213 |

**F 类 — 必出现的关键句（语义骨架）**

| # | 句子 | 来源 |
|---|------|---|
| S1 | "我们对 Anthropic 的工程团队怀有真诚的敬意" | L219 |
| S2 | "我们的批评同样真诚" | L219 |
| S3 | "设计理念和架构决策的生命力远比具体代码长" | L221 |
| S4 | **"让我们开始。"** | L223 |
| S5 | "→ 翻到 Part 1 第一章" | L241（CTA 引导词） |
| S6 | "这不是聊天机器人" | L241（CTA 目标章名） |

### 2.3 严禁出现的内容（边界纪律）

| 内容 | 来源类别 | 严禁原因 |
|------|---------|--------|
| 1,884 / 476,875 / 40 / 98 / 185 / 7 个 AI 实例 | Block 4 数字震撼 | 已经讲过，节奏破坏 |
| 9,000+ 星标 / claw-code 121K / harness-books 1.1k | Block 3 生态 | 已经讲过 |
| 12,000 字 NEW_INIT_PROMPT | 黄金资产钩子 | 见 §0.2 OQ4 |
| 11 类 Prompt 入口（A-K）+ 任何子类的具体行数 | 黄金资产 F9.1 | 见 §0.2 OQ5 |
| 25 个核心概念的具体名字 | 序章原文未提供 | 见 §0.2 OQ2 |
| 比喻家族的具体章节号（如 "Part 4 第 11 章"） | 序章原文未提供 | 见 §0.2 OQ3 |
| 任何"约 / 大约 / approx" 除了 "约 1,400" | FM-09 | 唯一例外 |
| Anthropic Logo / Claude Code Logo / 任何外部图片 | 安全 | 无外部资源 |

## 3. 视觉语言

### 3.1 推荐视觉组件（≥4 类）

| 组件 | 用途 | 优先级 |
|---|---|---|
| **5-metaphor-card-grid** | 5 比喻家族横向卡片网格，每张含 ICON（设计师自由）+ 名称 + 领域标签 + 子概念省略号串 | **P0 必出** |
| **pull-quote-large (Q1)** | 「好的比喻不是降低精度，是提供另一个入口。」单独成块，字号 3xl 起跳 | **P0 必出** |
| **principle-banner (Q2)** | 「比喻先行，精确随后。」+ 下方原文 blockquote 完整展开 | **P0 必出** |
| **5-group-tag-cards** | 0.5-A 速查表的 5 个分组标签卡（**只 5 个，不展开 25 概念**），可加"概念速查表（25 个）" 副标签 | P0 |
| **source-entry-pre-block** | 5 行源码入口等宽 `<pre>` 代码块，保留原文视觉对齐 | **P0 必出** |
| **acknowledgement-card** | "一个声明"段落卡片，浅色背景 + Anthropic 致谢 + 真诚批评双面 | P0 |
| **start-cta-button** | "→ 翻到 Part 1 第一章 · 这不是聊天机器人" 巨型按钮，全宽 | **P0 必出** |
| **horizontal-divider (--- 视觉)** | L239 分隔线保留为视觉水平线 | P1 |
| **footnote-mini (38 工具脚注)** | 解释 38/43 口径不同 | P1 |
| **let-us-begin-emphasis** | "让我们开始。" 单独一行字号大于正文，作为"声明卡"的收束 | P1 |

### 3.2 节奏建议（必须严格按这个顺序铺陈）

1. **第 1 段 — 比喻原则铺垫**：
   - "## 关于比喻" 标题
   - L196 段落（"这本书会大量使用比喻和类比……"）
   - **Q1 pull-quote 大字号**：「好的比喻不是降低精度，是提供另一个入口。」
2. **第 2 段 — 比喻方法论**：
   - "我们遵循一个原则" 引导
   - **Q2 principle-banner**：「比喻先行，精确随后。」+ 下方完整 blockquote
3. **第 3 段 — 5 比喻家族网格**：
   - "你会在书中看到几个反复出现的比喻家族" 引导
   - **5-metaphor-card-grid**（5 张卡片，桌面 5 列 / 平板 2 列 / 移动 1 列）
   - 每张卡片包含：ICON + 名称 + 领域标签 + 子概念省略号串（不要为它们补章节号，OQ3）
   - L211 "每个比喻第一次出现时都会完整展开" 一行小字
4. **第 4 段 — 0.5-A 速查表**：
   - 5-group-tag-cards（5 个分组标签卡，**不展开 25 概念，只写 "25 个核心概念分 5 组" 副标题**）
5. **过渡 — 水平分隔线**（L215 的 `---`）
6. **第 5 段 — 一个声明（acknowledgement-card）**：
   - "## 一个声明" 标题
   - L219 段落 + L221 段落
   - **结尾"让我们开始。" 大字号 emphasis**（独立一行）
7. **过渡 — 水平分隔线**（L225 的 `---`）
8. **第 6 段 — 快速导航：源码入口**：
   - "## 快速导航：源码入口" 标题
   - L229 引导句
   - **source-entry-pre-block**（5 行 mono 代码块，等宽对齐）
   - 38 工具旁挂 footnote-mini "(此处统计 src/tools/ 目录下的工具实现文件，与 0.3-A 的 43 个对外可调用工具在口径上不同)"
9. **最终 — 巨型 CTA 启程按钮**（L239 后的 L241）：
   - 上方水平分隔线（L239 的 `---`）
   - **start-cta-button** 全宽：上行 "→ 翻到 Part 1 第一章" 引导词，下行 "这不是聊天机器人" 章名（章名字号大于引导词），右侧箭头图标

### 3.3 字体/留白/对比度建议

- H2「关于比喻」/「一个声明」/「快速导航：源码入口」：`var(--cc-font-size-4xl)`，`var(--cc-font-weight-bold)`
- Q1 pull-quote：`clamp(1.875rem, 4vw, 3rem)` 字号，`var(--cc-leading-snug)`
- Q2 principle-banner：`var(--cc-font-size-3xl)`
- 5 比喻家族卡片标题：`var(--cc-font-size-2xl)`
- 5 比喻家族领域标签：`var(--cc-font-size-sm)`，颜色 `var(--cc-color-text-secondary)`
- 5 比喻家族子概念省略号串：`var(--cc-font-size-base)`
- 段落字号：`var(--cc-font-size-lg)`
- 段落行高：`var(--cc-leading-relaxed)`
- 段落最大宽度：`var(--cc-measure-prose)` 或 65-72ch
- "让我们开始。"：`var(--cc-font-size-3xl)`，居中或左对齐均可，必须独立一行
- source-entry-pre-block：`var(--cc-font-mono)`，`var(--cc-font-size-base)`，`white-space: pre`，背景 `var(--cc-color-surface-secondary)`
- start-cta-button：全宽（`width: 100%`），高度 `var(--cc-space-20)` 起步，背景 `var(--cc-color-brand)` 或 `var(--cc-color-surface-tertiary)`，hover 状态可加微动效
- block padding：`var(--cc-space-12)` 上下，`var(--cc-space-6)` 左右

## 4. 双源理解（本块只有 1 个图表预留 0.5-A）

### 4.1 VIS-0-015 / 0.5-A 概念速查表

**Brief 期望**（来自序章 L213 图表预留）：
- 期望承载的核心信息：25 个核心概念分 5 组
- 期望读者拿走的认知："读后续章节遇到陌生概念可以回这里查"

**HTML 现状**：
- ✗ **没有现成 HTML**。golden-assets 报告 + vis-soul 报告均未包含 0.5-A 的实现 HTML
- 这是序章原文的"图表预留"占位符，V1 阶段未生产

**Block 9 处理方式**：
- **不要做完整的 25 概念速查表**（OQ2 决断：不补全 25 概念）
- 只做"5 个分组标签卡"作为占位 / 锚点视觉，副标题写"25 个核心概念分 5 组（详见 0.5-A 概念速查表）"
- 5 个分组标签的 ICON / 颜色由 Sub-Agent 自由发挥
- 视觉权重：中等（不能盖过 5 比喻家族）

## 5. 评审 Checklist（评审 Sub-Agent 用，满分 65）

生成完成后，由独立的评审 Sub-Agent 用以下 checklist 打分（每项 5 分，硬性 0/5）：

| # | 检查项 | 评分依据 | 分值 |
|---|---|---|---|
| 1 | 5 比喻家族全在位（grep `操作系统` `机场安检` `城市财政` `三明治组装线` `游戏存档`） | 1 漏 = -3 分 | 5 |
| 2 | 5 个领域标签全在位（grep `全局框架` `安全模型` `Token 经济学` `提示词构建` `文件历史`） | 1 漏 = -2 分 | 5 |
| 3 | Q1「好的比喻不是降低精度，是提供另一个入口。」逐字 grep | 缺 = 0 分 | 5 |
| 4 | Q2「比喻先行，精确随后。」逐字 grep | 缺 = 0 分 | 5 |
| 5 | 7 硬数字精确（grep `5` `25` `2.1.88` `4,684` `38` `约 1,400`） | 任一漂移 = 0 分 | 5 |
| 6 | 5 行源码入口路径全在位（grep `src/main.tsx` `src/services/claude.ts` `src/tools/` `src/utils/permissions/` `src/memdir/`） | 1 漏 = -2 分 | 5 |
| 7 | "让我们开始。" 逐字 grep | 缺 = 0 分 | 5 |
| 8 | CTA 双关键词 grep `Part 1` + `这不是聊天机器人` | 缺 = -2 分 | 5 |
| 9 | 5 个分组标签全在位（grep `循环与执行` `工具与 Agent` `安全与权限` `上下文与 Token` `扩展与配置`） | 1 漏 = -2 分 | 5 |
| 10 | OQ2 守纪：grep 是否有 25 个具体概念名（如 "queryLoop" "Tool.ts" "permissions.ts" 等出现在"概念速查表"卡片内 → 违纪） | 凭空补 25 概念 = 0 分 | 5 |
| 11 | OQ4/OQ5 守纪：grep `12,000`、`12000`、`NEW_INIT_PROMPT`、`11 类`、`A-K`、`协调者/团队提示`、`输出风格提示` 全为 0 | 任一命中 = -3 分 | 5 |
| 12 | 严禁混入 Block 4 数字：grep `1,884` `476,875` `185` `9,000` `121K` 全为 0 | 任一命中 = -3 分 | 5 |
| 13 | 唯一"约"字 = "约 1,400"：grep `约` 次数 = 1 且必须紧邻 1,400 | 多余 = -2 分 | 5 |

**满分 65 分**，**≥52 分**才允许进入下一阶段融合。

**评审 Sub-Agent 必须额外报告**（不计分但写入 review-report.md）：
- "让我们开始。" 的字号是否真的大于正文
- 源码入口块是否用 `<pre>` 或等宽 mono（不是 grid）
- 38 工具脚注是否真的解释了 38/43 口径冲突
- 5 比喻家族卡片是否真的没有补全章节号
- 主观告别感评分（0-10）：第一眼是否被 "让我们开始。" + CTA 推动想翻页

## 6. 输出契约

- 文件名：`v2-demos/block9/prologue-block9-<kimi|minimax>-<timestamp>.html`
- 同时输出：稳定别名 `v2-demos/block9/prologue-block9-<kimi|minimax>-latest.html`
- 同时输出：`v2-demos/block9/prologue-block9-<kimi|minimax>-report.md`（生成 sub-agent 的自评报告）
- 文件大小预算：30-70 KB（HTML + inline CSS）
- 行数预算：500-1,000 行
- 所有数字 / 比喻 / 源码路径必须能 grep 到（不得 SVG 嵌入字符图片化）

## 7. 特殊提醒

- **Block 9 是收尾呼吸位，不是数字震撼**。Block 4 的"6xl 大字号 / 情绪顶点 / 全宽 break-the-grid"在这里**禁止**使用。这里的视觉权重应该是"温柔 / 真诚 / 启程"，不是"震撼 / 拍桌 / 高潮"。
- **5 比喻家族的卡片是本块灵魂**：如果做得好，读者会立刻被点亮 5 个不同的"思维入口"。如果做成 5 个枯燥的列表项，整个块就废了。
- **"让我们开始。" 是 Block 9 的情感锚点**：必须独立一行，字号大于正文。读者读到这一行，应该感到"好，我准备好了，翻页"。
- **巨型 CTA 是收尾的最后一击**：必须全宽 + 巨大 + 高对比度。沉淀整个序章的所有期待，让读者一眼就想点。
- **38 工具脚注是数字债防御**：必须出现，否则未来如果读者发现 0.1-A 的 40 / 0.3-A 的 43 / Block 9 的 38 不一致，会对整本书产生数字可信度系统性怀疑（alex-wang 评审 L38 警告）。
- **"约 1,400" 是序章唯一的"约"**：保留原文不动，但 Brief 不允许任何其它数字加"约"。
- **5 个分组标签卡不要做成"目录"**：不要写"点击进入分组"。0.5-A 速查表的真正落地是图表，不是按钮。卡片只是占位标签 + 副标题"25 个核心概念分 5 组"。
- **致敬 Anthropic 不是表演**：声明段落的真诚感很重要。不要把它做成"奖状卡"或"鸣谢墙"——是一个工程同行的平和叙事。
- **不要试图"集成"3 个子单元成一个 hero**：Block 9 必须是 3 + 1 的清晰段落结构（关于比喻 / 一个声明 / 源码入口 / CTA），不是"一张大图收尾"。

## 8. 与其他子 Agent 的协作约定

- 本 Brief 同时分发给 Kimi 主跑 + MiniMax 主跑 sub-agent（双跑模式）
- 两个 sub-agent **不得相互参照对方的产出**，独立设计
- 评审 sub-agent（第三个独立 sub-agent，建议用不同模型 cross-check）拿到两个产出 + 本 Brief 后用 §5 checklist 各打一份分
- 最终融合由 Opus 决定
- Block 9 是 V2 序章试水块**第二位**（第一位是 Block 3），本 Brief 写完后 meta-template 允许小改一次

---

*Brief by Opus on 2026-04-07 02:45 — 严格基于 prep 报告 + 序章原文 + Opus 7 OQ 决断，零虚构。*
