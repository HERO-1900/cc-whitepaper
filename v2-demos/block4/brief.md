# V2 序章 Block 4 · 数字震撼 · Brief

> 任务派发：双跑（Kimi 主跑 + MiniMax 主跑），独立产出后做交叉评审
> 块内职责：把"为什么这家产品能引爆生态"切换到"它到底有多大、多复杂"。这是 11 块叙事弧的**第一次情绪顶点**（Pentagram 式拍桌位）。读者必须在这一块"惊掉下巴"。
> 工时预算：5-7h（拆块方案 L39 列为次难第二位，含 2 张图同框 + 大字号巨型 stat-grid + pull-quote 阶梯）
> Brief 撰写人：Opus
> 创建时间：2026-04-07 02:30
> 实例化模板：handoff/v2-prologue-brief-template-2026-04-06.md
> 素材原料包：handoff/v2-prologue-block4-prep-2026-04-07.md（215 行，含 56 个硬数字 / 5 口径冲突 / 7 金句候选 / 2 张图 Gap）
> Opus 5 数字口径裁决（必须严格执行）：见 §0.1 数字纪律红线表

---

## 0. 给生成 Sub-Agent 的硬约束（最优先读）

**MUST**:
- 输出唯一一份完整 HTML 文件（含 `<!DOCTYPE html>`、`<head>`、`<style>`、`<body>`），无外部依赖
- token 引用：仅使用 `var(--cc-*)`，不得在本块内重新定义任何 `--cc-*` 变量
- 字体：`var(--cc-font-sans)` 正文 + `var(--cc-font-mono)` 数字/代码，不得出现任何系统字体名（违反 SOP FM-12）
- 数字精度：本 Brief 第 2.2 节列出的所有"硬数字"必须**逐位精确**，严禁约等于（不得出现 `~|约|大约|approx|K行|万行` 字样，违反 SOP FM-09）
- 配色：dark 主题为基线，浅色由 `[data-theme="light"]` 覆盖，两套都要可用
- 不得调用任何 JavaScript 框架或 CDN，纯 CSS + 极少量必要 vanilla JS（仅主题切换 + countUp 动画）
- 数字必须可被 grep 到（不得 SVG 嵌入数字图片化）
- 5 个 hero 数字必须用 `var(--cc-font-size-6xl)` 或更大字号（这是"情绪顶点"的核心证据）
- pull-quote「一个为 AI 智能体设计的操作系统」必须**单独成块、字号顶格**、视觉权重压过同页所有其它文本

**MUST NOT**:
- 严禁使用 `iframe`
- 严禁 `Math.random` / `while(...length<N)` 这类"凑数式数据补全"（违反 SOP FM-11）
- 严禁拿 LLM 自己虚构的数字；本 Brief 第 2.2/2.3 节没列出的数字一律不得自行编造
- 严禁删减 §2.2 列出的任何硬数字
- 严禁出现"生成式占位文本"（lorem ipsum / `// TODO` / `// 此处应有...`）
- 严禁加 Logo、人物头像等需要外部资源的元素
- **严禁混入白皮书创作元数据**（83 章 / 36 万字 / 122 图表 / 34 Harness 模块 等）—— 这些属于 Block 5 自我介绍。Block 4 数的是 Claude Code **本体**，不是数我们自己。一旦混淆，读者会产生"这是在数 Claude Code 还是在数你们"的迷惑（金矿警示 L52）
- 严禁混入 Block 3 的社区生态数字（9,000+ 星 / claw-code 121K / harness-books 1.1k 等）

### 0.1 数字纪律红线（5 口径冲突 Opus 已裁决，违者 0 分）

| # | 概念 | **必须用** | **严禁用** | 理由 |
|---|------|----------|----------|------|
| 1 | 文件总数（hero 主位） | **1,884**（只算 .ts + .tsx） | 1,902（含 18 个 .js） | 守序章 L57 加粗。1,902 仅允许出现在 VIS-0-002 微图表的小字注脚，禁止与 1,884 同框并列 |
| 2 | 工具数（hero 主位） | **40** | 38 / 43 | 序章 L61 + 扫描 S11 双源吻合。38 留给 Block 9 解释（核心 src/tools/ 不含 Agent 内置），43 是 harness-toolkit 对外目录 |
| 3 | 有效代码行（hero 主位） | **476,875** | 512,695 主位 / "约 47 万" / "近 50 万" | 守序章 L61 加粗。512,695 仅允许做小字注脚"含空行 512,695"，主位必须 476,875 |
| 4 | Prompt 数（hero 主位，升档使用） | **185** | 24 / 54 / 12 等 P 类细分独立做主数字 | 黄金资产 P1，引用 prompt-audit-full.md。Block 4 升档把 4 数字 stat-grid 扩到 5 数字，185 是第 5 位 hero |
| 5 | Agent 实例数（如使用） | **7**（一次回复并发 7） | 12（源码内置清单） | 7 是序章 L76 加粗"行为数字"，12 是 Block 5/6 的"目录数字"，语义不同不得混合 |

任何评审 sub-agent 一旦发现产出违反上表，视为 §5 评审 checklist 第 1 项 0 分。

---

## 1. 块在 11 块叙事弧中的位置

```
块1 → 块2 → 块3 → [块4] → 块5 → 块6 → 块7a → 块7b → 块7c → 块8 → 块9
hero  反差  生态   数字    使命  OS建立 安全  接口  内部  分流  收尾
```

- **本块**：第 4 拍
- **节奏角色**：★ **第一次情绪顶点 / Hero 级数字震撼 / 全篇最高潮**。Pentagram 式"安静开场 → 慢慢推进 → 第一次拍桌子"的拍桌位。
- **上一块给我交接的"读者情绪"**：刚看完 6 个开源项目排行榜 + ccunpacked.dev / harness-books 双姊妹特写，处于"原来一次源码泄露催生了一整个生态"的轻度兴奋，自然想问"那这家产品的源码到底有多大？"
- **我要交给下一块的"读者情绪"**：被 5 个巨数 + 一句"操作系统"金句锤到震撼 → 然后被 Q4/Q5 的"类比 caveat"轻轻拉回理性 → 需要把"震撼"递为"好，这本书要帮我看清这一切"的使命感。下一块（Block 5）会接住这个使命感。

## 2. 内容承载

### 2.1 原文（来自 `book/part0_序章/00_序章.md` 第 57-66 行）

```markdown
你打开的是一个拥有 **1,884 个 TypeScript 文件** 的工程系统。它有自己的进程调度器、安全沙箱、文件版本管理、多实例协调器、遥测管线、插件生态——如果你非要用一个词来描述它，最准确的说法是：

**一个为 AI 智能体设计的操作系统。**

> **[图表预留 0.1-A]**：CC 源码全量统计仪表盘——476,875 行有效代码、1,884 个 TS 文件、40 个 AI 工具、98 个斜杠命令的穷举式全景数字。

只不过它管理的不是硬件进程，而是 AI 智能体的生命周期。当然，"操作系统"是一个帮助理解的类比，不是精确的技术定义——Claude Code 运行在 Node.js 之上，本身仍是一个用户态应用程序。但它承担的**职责**（资源调度、安全隔离、生命周期管理、扩展机制）确实和操作系统高度同构。这个类比的价值不在于字面准确，而在于它提供了一个你已经熟悉的思维框架来理解一个全新的系统。

> **[图表预留 0.1-B]**：Claude Code 英雄架构图——9 节点主链路（CLI→权限→Prompt→Query Loop→API→工具→输出）与 6 个支撑子系统（权限/配置/MCP/Hooks/沙箱/上下文）的全局俯瞰。
```

> **额外引用**（可斟酌使用，来自 `00_序章.md:76`，序章正文加粗）：
>
> 「为什么一条看似简单的回复背后，可能有 **7 个 AI 实例**在同时工作」

### 2.2 必保留事实清单（不得删，不得简化，逐位精确）

**A 类 — Hero 5 巨数 stat-grid（`var(--cc-font-size-6xl)` 字号铺开，必出现）**

| # | 数字 | 含义 | 来源 file:line | Opus 提示 |
|---|------|------|---|---|
| H1 | **1,884** | TypeScript 文件数（.ts 1,332 + .tsx 552） | `book/part0_序章/00_序章.md:57` 加粗 + `:61` | 序章正文唯一加粗的总文件数。**Stat-grid 第一位**。可以加二级注释 "1,332 .ts + 552 .tsx" |
| H2 | **476,875** | 有效代码行数（非空行） | `book/part0_序章/00_序章.md:61` + `cc-source-stats-report.md:29` | **字号最大**。可加小字注脚 "（含空行共 512,695）" |
| H3 | **40** | AI 工具数 | `book/part0_序章/00_序章.md:61` + `cc-source-stats-report.md:181` | 严禁写 38 或 43 |
| H4 | **98** | 斜杠命令数 | `book/part0_序章/00_序章.md:61` + `cc-source-stats-report.md:335` | 可加二级注释 "86 子目录 + 12 根文件" |
| H5 | **185** | 全源码独立 Prompt 单元数 | `handoff/prompt-audit-full.md:6` + `handoff/golden-assets-prologue-mining-2026-04-06.md:17` | **本块升档新增**。来源是 P001-P183 + P101a/P101b 穷尽审计。可在数字旁加 "穷尽审计" 字样 |

**B 类 — 必保留金句（pull-quote 阶梯，按顺序出现）**

| # | 金句 | 来源 | 视觉处理建议 |
|---|------|------|----|
| Q-pre | 「它有自己的进程调度器、安全沙箱、文件版本管理、多实例协调器、遥测管线、插件生态——如果你非要用一个词来描述它，最准确的说法是……」 | `00_序章.md:57-58` | break-the-grid 全宽引言，做"反转铺垫"，停顿一拍 |
| **Q1** | 「**一个为 AI 智能体设计的操作系统。**」 | `00_序章.md:59` | **巨型 pull-quote**，字号 6xl 起跳，单独成块，本块视觉权重最高的一行。位置：5 巨数下方 + 双图上方 |
| Q3 | 「只不过它管理的不是硬件进程，而是 AI 智能体的生命周期。」 | `00_序章.md:63` | Q1 下方小字补刀，避免"操作系统"被过度放大 |
| Q4 | 「"操作系统"是一个帮助理解的类比，不是精确的技术定义——Claude Code 运行在 Node.js 之上，本身仍是一个用户态应用程序。但它承担的**职责**（资源调度、安全隔离、生命周期管理、扩展机制）确实和操作系统高度同构。」 | `00_序章.md:63` | **块尾呼吸位**，必须出现，用浅色 caveat-card 包装 |
| Q5 | 「这个类比的价值不在于字面准确，而在于它提供了一个你已经熟悉的思维框架来理解一个全新的系统。」 | `00_序章.md:63` | 与 Q4 同框，作为 caveat-card 第二段 |

**C 类 — 必出现的两张图位**

| ID | 图名 | 来源 HTML（参考） | Brief 期望 |
|---|---|---|---|
| VIS-0-002 / 0.1-A | 源码统计仪表盘 | `test-viz/production/html/VIS-0-002_源码统计仪表盘.html`（736 行，旧评 8/10） | 上 6 KPI 卡片 + 下 6 微图表网格，KPI 用 countUp 动画。**V2 必须修正：旧版 KPI 用 "~250K 行代码"，新版必须改为 476,875 + 1,884** |
| VIS-0-001 / 0.1-B | 英雄架构图 | `test-viz/production/html/VIS-0-001_英雄架构图.html`（255 行，旧评 6/10） | 9 节点主链路 + 6 子系统虚线辅连 + 暗色 #0d1117 底色 + 多粒子流动 |

**两张图必须"双图同框"**：左 VIS-0-002 仪表盘（数字证据） / 右 VIS-0-001 架构图（结构证据），并列 12 栅格。这是拆块方案 L122-126 的明确指定。

### 2.3 可灵活使用的次级数字（B 阶 stat row，sub-agent 按需挑选 ≤ 6 个，禁止全部塞入）

> **纪律**：A 类 Hero 5 巨数为主战场。次级数字若使用，**字号必须显著小于 H1-H5**（建议 `var(--cc-font-size-3xl)` 或更小），位置在 hero 下方独立 row，目的是"细节佐证"而非"数字洪水"。
> 一定不要把这一节当 checklist 全部塞进去——读者会因数字过载而失去焦点。

| # | 数字 | 含义 | 来源 | 适合做什么 |
|---|------|------|---|---|
| B1 | **35,799** | 空白行数 | `cc-source-stats-report.md:31` | 配合 H2 算 "93.0% 有效代码率"（476,875 / 512,695 ≈ 0.9301） |
| B2 | **35** | 顶层子系统数 | `cc-source-stats-report.md:32` | "Claude Code 被分成 35 个顶层子系统" 一行注释 |
| B3 | **180,472** | utils 子系统行数（占 35.2%） | `cc-source-stats-report.md:37` | "光一个 utils 就占全项目 1/3" pull-quote 候选 |
| B4 | **5,594** | 最大文件 cli/print.ts 行数 | `cc-source-stats-report.md:95` | "最大单文件" 标签 |
| B5 | **4,684** | main.tsx 行数（序章 L232 提到的"巨型文件"） | `cc-source-stats-report.md:98` | "启动入口巨型文件" 标签 |
| B6 | **4,709** | 安全关键词出现次数 | `cc-source-stats-report.md:162` | "Security is first-class citizen" 数字证据 |
| B7 | **621 / 32.7%** | 涉及安全关键词的文件占比 | `cc-source-stats-report.md:163` | "近 1/3 文件涉及安全" 一行 |
| B8 | **2,363** | type 定义数（vs 83 个 interface） | `cc-source-stats-report.md:414` | "type 比 interface 多 28.5 倍" 风格细节 |
| B9 | **1,666** | Zod schema 使用处 | `cc-source-stats-report.md:417` | "1,666 处 Zod" 严谨度 |
| B10 | **5,469** | await 表达式数 | `cc-source-stats-report.md:482` | "5,469 个 await，I/O 密集到什么程度" |
| B11 | **1,931** | try 块数 | `cc-source-stats-report.md:433` | "近 2 千个 try/catch 防御密度" |
| B12 | **138 / 0 / 0** | TODO 138 个，FIXME 0 个，HACK 0 个 | `cc-source-stats-report.md:402-404` | **反差金句候选**："138 个 TODO，零个 FIXME，零个 HACK" |

**P 类（Prompt 维度，扩展 H5 用）**——仅在做"185 Prompt 拆解" sub stat 时使用：

| # | 数字 | 含义 | 来源 |
|---|------|------|---|
| P-A | **24** | 系统提示段落数（A 类） | `prompt-audit-full.md:14` |
| P-B | **54** | 工具描述/Prompt 数（B 类，35 工具家族） | `prompt-audit-full.md:15` |
| P-C | **12** | 内置 Agent 提示数（C 类） | `prompt-audit-full.md:16` |
| P-D | **20** | Skill 模板数（D 类） | `prompt-audit-full.md:17` |
| P-E | **19** | 服务层提示数（E 类） | `prompt-audit-full.md:18` |
| P-F | **12** | 记忆系统提示数（F 类） | `prompt-audit-full.md:19` |
| P-Long | **12,000** 字 | 最长单一 Prompt（P179 NEW_INIT_PROMPT，8 阶段引导流程） | `prompt-audit-full.md:750, 660` + golden-assets N16 |

**Agent 行为数字（最多用 1 个）**：
- **7** —— "一次看似简单的回复背后，可能有 7 个 AI 实例同时工作"，来源 `00_序章.md:76`。如果用，建议作为 hero 下方的 "行为数字" 单独一行，标签写"一次回复 / 7 个并发实例"。**严禁写 12**。

### 2.4 严禁混入的数字（金矿 L52 + alex-wang 评审 L38 红线）

| 数字 | 类别 | 严禁原因 |
|------|------|---------|
| 83 章 / 36.0 万字 / 122 图表 / 34 Harness 模块 / 74 章 / 1515 段 | 白皮书创作元数据 | 属于 Block 5 自我介绍。一旦和 Claude Code 本体数字并列读者会迷惑 |
| 9,000+ 星标 / claw-code 121K / harness-books 1.1k | 社区生态数据 | 属于 Block 3，已经讲过 |
| 1,902（在 hero 主位） | 文件总数错口径 | 见 §0.1 数字纪律 |
| 38 / 43（工具数） | 工具数错口径 | 见 §0.1 数字纪律 |
| 12（Agent 实例数） | Agent 错口径 | 见 §0.1 数字纪律 |
| 250K / "约 47 万行" / "近 50 万行" | 模糊数字 | FM-09 严禁约等于；H2 必须 476,875 |

## 3. 视觉语言

### 3.1 推荐视觉组件（≥4 类，本块是情绪顶点必须密集出场）

| 组件 | 用途 | 优先级 |
|---|---|---|
| **mega-stat-grid (5 列)** | H1-H5 五个 hero 巨数同框，字号 6xl 起跳，countUp 动画 | **P0 必出** |
| **break-the-grid pull-quote (Q1)** | 「一个为 AI 智能体设计的操作系统」单独成块、视觉权重最高 | **P0 必出** |
| **dual-chart frame (双图同框)** | 左 VIS-0-002 仪表盘 / 右 VIS-0-001 架构图，并列 12 栅格 | **P0 必出** |
| **caveat-card (Q4 + Q5)** | 块尾呼吸位的"类比限制"caveat 卡片，浅色降权 | **P0 必出** |
| **secondary-stat-row** | B 类次级数字 ≤ 6 个的横向排布，字号 3xl，hero 下方 | P1 |
| **prompt-breakdown card** | 185 = 24+54+12+20+19+12 + 还有 44 个杂项，配 P-Long 12000 字震撼 | P1 |
| **timeline-mini "1.2 万字 Prompt 拆解"** | 把 P-Long 12000 字做成"一个命令背后的引导流程长度" | P2 |
| **stat-row "0 FIXME / 0 HACK / 138 TODO"** | B12 反差细节 | P2 |
| **highlight-chip "93.0% 有效代码率"** | 用 H2/B1 算出的衍生数字 | P2 |

### 3.2 节奏建议（必须严格按这个顺序铺陈）

1. **块开头 — 反转铺垫**：Q-pre 全宽 break-the-grid 引言（"如果你非要用一个词来描述它……"），停顿一拍，留白
2. **第一波拍桌 — 5 巨数 mega-stat-grid**：H1-H5 五个 6xl 数字 countUp 动画铺开，数字下方 H2 配 "（含空行 512,695）" 小字
3. **第二波拍桌 — Q1 巨型 pull-quote**："一个为 AI 智能体设计的操作系统"，字号 6xl，单独成块，本块视觉权重最高
4. **补刀**：Q3 小字 "只不过它管理的不是硬件进程，而是 AI 智能体的生命周期"
5. **数字证据 + 结构证据双图同框**：左 VIS-0-002 仪表盘（含 KPI 上 6 + 微图下 6） / 右 VIS-0-001 架构图（9 节点主链路 + 6 子系统）
6. **次级 stat row（可选）**：B 类挑 4-6 个，横排，字号小于 hero
7. **可选第三波 — Prompt 拆解卡**：H5 = 185 = 24+54+12+20+19+12+...，配 P-Long 12000 字震撼
8. **块尾呼吸 — caveat-card (Q4 + Q5)**：浅色降权，把读者从"震撼"拉回"理性"，递给 Block 5 使命感

### 3.3 字体/留白/对比度建议（仅 token 引用层面）

- H2「数字震撼」/「打开引擎盖」：`var(--cc-font-size-5xl)` 起跳，`var(--cc-font-weight-bold)`
- 5 hero 数字（H1-H5）：`var(--cc-font-size-6xl)` 起跳（建议尝试更大，例如 `clamp(4rem, 12vw, 10rem)`），`var(--cc-font-mono)`，颜色 `var(--cc-color-brand)`
- Q1 巨型 pull-quote："一个为 AI 智能体设计的操作系统"：`clamp(2.5rem, 6vw, 5rem)` 字号，`var(--cc-leading-snug)`，颜色 `var(--cc-color-text-primary)`，居中或左对齐均可，但必须破栅格全宽
- 段落字号：`var(--cc-font-size-lg)`
- 段落行高：`var(--cc-leading-relaxed)`（1.7-1.8 区间）
- 段落最大宽度：`var(--cc-measure-prose)` 或 65-72ch
- caveat-card 字号：`var(--cc-font-size-base)`，背景色取 `var(--cc-color-surface-secondary)`，**字号必须显著小于 hero**，让读者感觉到"语气降下来了"
- 双图同框 gap：`var(--cc-space-8)` 起步，移动端折叠为 1 列
- block padding：`var(--cc-space-16)` 上下，`var(--cc-space-6)` 左右（Block 4 是情绪顶点，padding 比其它块更大）

## 4. 双源理解（本块包含 2 张图，必须严格遵守双源原则）

> 双源原则：原始 Brief（设计意图）+ 当前 HTML（V1 实现）必须同时被 sub-agent 理解。差距即关键信息。Sub-agent **不要从 V1 HTML 直接 copy**，必须用 Brief 期望覆盖 V1 缺陷。

### 4.1 VIS-0-002（0.1-A）源码统计仪表盘

**Brief 期望**（来自 `web/handoff/v2-prologue-vis-soul-2026-04-06.md:71-103` + 拆块方案）：
- 期望视觉语言：上 6 KPI 卡片 + 下 6 微图表网格，KPI 用 countUp 动画，IntersectionObserver 懒触发
- 期望承载的核心信息：穷举式全景数字（476,875 / 1,884 / 40 / 98）+ Top10 模块行数排行
- 期望读者拿走的认知："这不是一个小工具，这是一个 50 万行的工程系统"

**HTML 现状**（V1 评分 8/10，`web/test-viz/production/html/VIS-0-002_源码统计仪表盘.html`，736 行）：
- 实现得分（自评）：8/10
- 命中 Brief 的地方：上 6 下 6 双区结构 / Top10 模块数据 / countUp 动画 / 响应式
- **偏离 Brief 的地方（必须修正）**：KPI 用了 "~250K 行代码"，但序章正文明确 476,875 → V2 必须改为 **476,875**

**Gap 分析**（vis-soul:97-101）：
- 必保留特征：
  1. 上 6 下 6 双区结构
  2. Top10 模块数据（queryLoop.ts / Tool.ts / permissions.ts 领衔），行数数字要准确
  3. KPI 数字必须 countUp 动画
- **必修正缺陷**：
  - "~250K 行代码" → **476,875 行**（不要 "~"，不要 "K"）
  - KPI 数字可从 6 个扩展到 4 个：476,875 行 / 1,884 文件 / 40 工具 / 98 斜杠命令（与 Block 4 hero 配方一致）
- V2 改造建议：**复用 + 微调 + 精确数字修正**。结构沿用 V1，数字必须精确化。

### 4.2 VIS-0-001（0.1-B）英雄架构图

**Brief 期望**（来自 `web/handoff/v2-prologue-vis-soul-2026-04-06.md:41-69`）：
- 期望视觉语言：9 节点主链路 + 6 大子系统，暗色主题，多粒子流动，hover 子系统弹窗，节点可点击跳转章节
- 期望承载的核心信息：CLI → 权限 → Prompt → Query Loop → API → 工具 → 输出 的全局俯瞰
- 期望读者拿走的认知："这家产品有清晰的架构，不是黑魔法堆出来的"

**HTML 现状**（V1 评分 6/10，`web/test-viz/production/html/VIS-0-001_英雄架构图.html`，255 行）：
- 实现得分（自评）：6/10
- 命中 Brief 的地方：9 节点主链路结构正确 / 暗色 #0d1117 底色
- **偏离 Brief 的地方**：只有一颗粒子 / 无 hover 交互 / 无子系统说明卡 / 无入场动画

**Gap 分析**（vis-soul:65-69）：
- 必保留特征：
  1. 9 节点主链路完整（含工具调度器→响应解析器→API 客户端右侧闭环）
  2. 橙色系专属 Agent Loop 回环标注
  3. 六大子系统以虚线辅连
  4. 暗色主题 #0d1117 底色
- 必修正缺陷：单粒子 → 多粒子流动 / 无 hover → 子系统弹窗 / 无入场动画 → 节点从模糊→清晰
- V2 改造建议：**实质重做**。结构骨架沿用 V1，但视觉效果（粒子 / hover / 入场动画）必须升级。可用纯 CSS 多粒子（`@keyframes` 多个 path）实现，不引入 Canvas/WebGL。

### 4.3 双图同框的栅格约定

- 桌面（≥1024px）：左仪表盘 / 右架构图，并列 12 栅格（左 7 / 右 5 或 6/6）
- 平板（768-1023px）：仍可并列，但栅格调整为 12/12
- 移动（<768px）：上下堆叠，仪表盘在上，架构图在下
- 两张图共享同一 dark/light 主题切换逻辑

## 5. 评审 Checklist（评审 Sub-Agent 用，满分 70）

生成完成后，由独立的评审 Sub-Agent（不同模型 cross-check）用以下 checklist 打分（每项 1-5 分或硬性 0/5）：

| # | 检查项 | 评分依据 | 分值 |
|---|---|---|---|
| 1 | A 类 5 巨数全在位（grep `1,884` `476,875` `40` `98` `185`） | 1 漏 = -3 分 | 5 |
| 2 | 5 巨数逐位精确（grep `1,884` `476,875` `185` 三者有 1 个写错就 0） | 任 1 个数字漂移 = 整项 0 分 | 5 |
| 3 | §0.1 数字纪律红线（grep 全文不得出现 `1,902` `~250K` `约 47` `近 50 万` `38 个工具` `43 个工具` 在 hero 主位） | 命中 1 次 = 0 分 | 5 |
| 4 | 全文 grep `~|约|大约|approx|K行|万行` 必须为 0（FM-09） | 命中 1 次 = 0 分 | 5 |
| 5 | Q1 巨型 pull-quote「一个为 AI 智能体设计的操作系统」必须存在且字号 ≥ 4rem | 字号不达 = -2 分 / 缺失 = 0 分 | 5 |
| 6 | Q-pre / Q3 / Q4 / Q5 四条金句全在位 | 1 漏 = -1 分 | 5 |
| 7 | VIS-0-002 仪表盘存在且 KPI 数字精确（476,875 + 1,884 + 40 + 98 全部出现） | 缺 1 个 = -2 分 | 5 |
| 8 | VIS-0-001 架构图存在且 9 节点主链路完整 | 缺节点 = -2 分 | 5 |
| 9 | 双图同框（桌面端并列） | 不满足 = -2 分 | 5 |
| 10 | 视觉组件 ≥ 4 类（mega-stat-grid + Q1 pull-quote + dual-chart + caveat-card 四个 P0 必出） | 1 类缺 = -2 分 | 5 |
| 11 | token 纪律：`var(--cc-*)` 出现次数 ≥ 100 | 不达 = 减半 | 5 |
| 12 | 字体：仅用 `var(--cc-font-sans)` + `var(--cc-font-mono)`（FM-12） | 出现其它字体名 = 0 分 | 5 |
| 13 | 严禁混入白皮书元数据（83 章 / 36 万字 / 122 图表 / 34 Harness 模块）grep 全文必须为 0 | 命中 1 次 = -3 分 | 5 |
| 14 | 严禁混入 Block 3 社区数字（9,000 / 121K / 1.1k / claw-code / harness-books / byterover / ccunpacked）必须为 0 | 命中 1 次 = -3 分 | 5 |

**满分 70 分**，**≥56 分**才允许进入下一阶段融合。

**评审 Sub-Agent 必须额外报告**（不计入分数，但写入 review-report.md）：
- 5 巨数的 grep 命中行号
- 是否使用了 §2.3 次级数字（用了哪几个、字号是否小于 hero）
- 是否使用了 P 类 Prompt 拆解卡（用了哪几个）
- "情绪顶点感"主观评分（0-10）：第一眼是否被震到

## 6. 输出契约

- 文件名：`v2-demos/block4/prologue-block4-<kimi|minimax>-<timestamp>.html`
- 同时输出：稳定别名 `v2-demos/block4/prologue-block4-<kimi|minimax>-latest.html`
- 同时输出：`v2-demos/block4/prologue-block4-<kimi|minimax>-report.md`（生成 sub-agent 的自评报告）
- 文件大小预算：60-120 KB（含两张内嵌图，比 Block 3 大）
- 行数预算：1,000-1,800 行
- 所有数字必须能 grep 到（不得 SVG 嵌入数字图片化）

## 7. 特殊提醒

- **本块是 V2 序章的"全篇情绪顶点"**——MiniMax 的旧版 §4「数字震撼」（行 859-915）KPI 卡片结构是历史最佳，新版必须**至少不输于 MiniMax 旧版**。拆块方案 L129 明确写"不能输给 MiniMax §4"。
- **Q1 是全块灵魂**：「一个为 AI 智能体设计的操作系统」这一行的视觉权重必须**压过同页其它所有文字**。如果 Sub-Agent 把它做成普通段落或者小号引用，整个块就废了。
- **5 巨数的字号竞赛**：H1-H5 五个数字必须**显著大于**所有其它文字，包括 H2 标题。Sub-Agent 不要"克制"——情绪顶点就是要"大到俗气"，但保持 token 纪律。
- **double-chart 是栅格挑战**：双图同框在 12 栅格内并列，移动端折叠，是本块技术难度最高的部分。如果硬塞容易破图，建议先做 mock 桌面/平板/移动三套截图再 commit。
- **caveat 不能省**：很多 sub-agent 会觉得 Q4/Q5 的 "类比 caveat" 削弱了情绪，倾向于砍掉。**严禁砍**——这是序章原文 L63 的明确学术姿态，砍了等于掩盖原文的严谨度。
- **不要"数字洪水"**：B 类次级数字最多挑 4-6 个，**P 类最多挑 1 个 Prompt 拆解卡**。不要把所有数字都塞进去，那会让读者从"震撼"变成"麻木"。"少即是多" 是情绪顶点的反直觉纪律。
- **"7 个 AI 实例"是行为数字**：序章 L76 加粗，建议作为 hero 下方独立行（"一次回复 / 7 个并发实例"），不要混进 5 巨数 stat-grid。
- **VIS-0-002 旧版的 "~250K" 必须修正**：这是 vis-soul:93 明确指出的 V2 必修缺陷。任何沿用旧 HTML 的 sub-agent **必须 grep 删除 "250K" 字样**。
- **VIS-0-001 不要追求 Canvas/WebGL**：V2 多粒子流动用纯 CSS `@keyframes` 即可，不要引入额外依赖。

## 8. 与其他子 Agent 的协作约定

- 本 Brief 同时分发给 Kimi 主跑 + MiniMax 主跑 sub-agent（双跑模式）
- 两个 sub-agent **不得相互参照对方的产出**，独立设计
- 评审 sub-agent（第三个独立 sub-agent，建议用不同模型 cross-check）拿到两个产出 + 本 Brief 后用 §5 checklist 各打一份分
- 最终融合由 Opus 决定（可能选其中一个、可能拼装两者；如果 Q1 pull-quote 一个做得更好、stat-grid 另一个做得更好，Opus 可以请第三个 sub-agent 拼装）
- Block 4 是"试水块 + 情绪顶点"双重身份，本 Brief 写完后 meta-template 允许小改一次

---

*Brief by Opus on 2026-04-07 02:30 — 严格基于 prep 报告 + 序章原文 + Opus 5 数字裁决，零虚构。*
