# V2 序章 Block 7a · 安全防线 · Brief

> 任务派发：双跑（Kimi 主跑 + MiniMax 主跑），独立产出后做交叉评审
> 块内职责：**Block 7 信息密度顶点的第 1 拍 / OS 类比"安全模型"行的全面展开**。从"安全是一个开关"切到"安全是矩阵化、可配置、有兜底的纵深体系"。
> 工时预算：3-4h（含 2 张图 0.3-B 安全矩阵 + 0.3-C 权限对照表）
> Brief 撰写人：Opus
> 创建时间：2026-04-07 04:20
> 实例化模板：handoff/v2-prologue-brief-template-2026-04-06.md
> 素材原料包：handoff/v2-prologue-block7-prep-2026-04-06.md（§二 Block 7a 段落）
> Opus 5 OQ 决断：见 §0.2

---

## 0. 给生成 Sub-Agent 的硬约束（最优先读）

**MUST**:
- 输出唯一一份完整 HTML 文件，无外部依赖
- token 引用：仅 `var(--cc-*)`，字体 `var(--cc-font-sans)` + `var(--cc-font-mono)`
- 数字精度：所有数字逐位精确（FM-09）
- dark 主题为基线 + light 主题覆盖
- 不调用 JS 框架 / CDN
- **L116 OS 类比表行必须保留**：「**安全模型 (Security)** | 权限系统 + 沙箱 + 企业策略 | 十步权限状态机、seatbelt/bwrap 进程隔离、bypass-immune 规则——多层防御纵深」（OQ1 选项 B：摘取相关行作 Block 7a 开头小卡片）
- **L118 小区门禁通俗理解必须保留**：「💡 **通俗理解**：权限系统就像**小区门禁**——业主（已批准操作）= 自动放行，访客（未知操作）= 要登记确认，外卖（沙箱内命令）= 扫码进，可疑人员（危险操作）= 直接拒绝。」
- **VIS-0.3-B 安全矩阵必出**：4 行防御（权限系统 / 沙箱隔离 / 命令预检 / 企业策略）× 3 列威胁（Prompt 注入 / 文件越权 / 命令逃逸）= 12 单元格，三色编码（强=#3fb950 / 中=#d29922 / 弱=#f85149）
- **VIS-0.3-C 权限对照表必出**：6 种权限模式（plan / default / acceptEdits / autoAccept / bypass / dontAsk）× 工具行为矩阵（Read / Edit / Bash / Agent + 等），三色编码 + default 列高亮
- **块尾过渡到 Block 7b**：「但 Claude 不只有内置工具。它还能接入外部工具、被外部钩子拦截、被第三方扩展加新能力——这些'对外的延展面'是另一组完全不同的设计。」

**MUST NOT**:
- 严禁 `iframe` / `Math.random` / 凑数式数据补全
- 严禁拿 LLM 自己虚构的安全机制 / 权限模式 / 工具
- 严禁混入 Block 1-6 / Block 8-9 任何元素
- 严禁混入 Block 4 hero (1,884 / 476,875 / 40 / 98 / 一个为 AI 智能体设计的操作系统)
- 严禁混入 Block 5 (这不是使用指南 / 这是工程解剖)
- 严禁混入 Block 6 全文 (三件事面板 / 内核 / Syscall / 43 工具目录 / 课程关联)
- **严禁展开 Block 7b 内容**（MCP / Hooks / Skills / 扩展机制）
- **严禁展开 Block 7c 内容**（配置层 / 模块依赖图）
- 严禁展开 OS 类比表的其它 9 行（只摘安全模型一行）
- 严禁混入 Block 8/9 (三类读者 / 5 比喻家族 / let us begin)
- 严禁视觉冲击力压过 Block 4

### 0.1 Opus 5 OQ 决断（必须严格执行）

| OQ | 议题 | Opus 决断 |
|----|------|---------|
| **OQ1** | OS 类比表如何拆 | **选项 B**：表格在 Block 6 出现一次，Block 7a 摘取「安全模型 (Security)」行作为开头小卡片 + 添加"**OS 类比锚点**"标签 |
| **OQ2** | Block 7b 是否分流 | **不分流**（保持 7a/7b/7c 三拆原方案） |
| **OQ3** | VIS-0.3-D 视觉纠错 | **本块不涉及 D**，但 Block 7a 的 0.3-B 也要确保按 Brief 期望（4×3 矩阵 + 12 单元格）实现 |
| **OQ4** | 风险等级配色 | **强=绿 #3fb950 / 中=黄 #d29922 / 弱=红 #f85149**（与现有 HTML 一致） |
| **OQ5** | default 模式高亮 | **必出**：default 列用蓝色边框 + "推荐"标签 |

---

## 1. 块在 11 块叙事弧中的位置

```
块1 → 块2 → 块3 → 块4 → 块5 → 块6 → [块7a] → 块7b → 块7c → 块8 → 块9
hero  反差  生态  数字  使命  OS建立 安全     接口   内部   分流  收尾
```

- **本块**：第 7 拍，三拆中的第 1 拍
- **节奏角色**：★ **Block 7 信息密度顶点的第 1 拍 / 安全防线技术深潜**
- **上一块（Block 6 OS 建立）交接情绪**：刚被「OS 类比表已经把概念坐标系建立起来。但有些子系统重要到一句类比说不清——它们需要图。我们从最关键的'安全'开始。」推动到"准备深潜安全"
- **我要交给下一块（Block 7b 外部接口）的情绪**：把"安全是矩阵化的纵深体系" 递为 "扩展接口是完全不同的另一组设计"

## 2. 内容承载

### 2.1 原文（来自 `book/part0_序章/00_序章.md` 第 116-122 行）

```markdown
| **安全模型 (Security)** | 权限系统 + 沙箱 + 企业策略 | 十步权限状态机、seatbelt/bwrap 进程隔离、bypass-immune 规则——多层防御纵深 |

> 💡 **通俗理解**：权限系统就像**小区门禁**——业主（已批准操作）= 自动放行，访客（未知操作）= 要登记确认，外卖（沙箱内命令）= 扫码进，可疑人员（危险操作）= 直接拒绝。

> **[图表预留 0.3-B]**：安全模型全景图——四层纵深防御（权限 / 沙箱 / 命令预检 / 企业策略）× 三种威胁向量（Prompt 注入 / 文件越权 / 命令逃逸）的全景对照。

> **[图表预留 0.3-C]**：权限模型对照表——6 种权限模式（plan / default / acceptEdits / autoAccept / bypass / dontAsk）在各类工具（Read / Edit / Bash / Agent）上的"自动/确认/禁止"矩阵。
```

### 2.2 必保留事实清单

**A 类 — OS 类比表行（开头锚点小卡片）**

| OS 概念 | CC 对应物 | 类比成立理由 |
|---|---|---|
| **安全模型 (Security)** | 权限系统 + 沙箱 + 企业策略 | 十步权限状态机、seatbelt/bwrap 进程隔离、bypass-immune 规则——多层防御纵深 |

**B 类 — 必出 1 段「💡 通俗理解」**

| 类比 | 内容（逐字保留）|
|---|---|
| **小区门禁** | 业主（已批准操作）= 自动放行，访客（未知操作）= 要登记确认，外卖（沙箱内命令）= 扫码进，可疑人员（危险操作）= 直接拒绝 |

**C 类 — VIS-0.3-B 安全模型全景图（必出图 1）**

| 字段 | 值 |
|---|---|
| 标题 | 安全模型全景图 / 四层防御 × 三种威胁的安全矩阵 |
| 4 层防御 (行) | **权限系统 / 沙箱隔离 / 命令预检 / 企业策略** |
| 3 类威胁 (列) | **Prompt 注入 / 文件越权 / 命令逃逸** |
| 12 单元格 | 每格标具体防护措施 + 有效性强/中/弱 + 三色编码 |
| 三色 | 强 #3fb950 / 中 #d29922 / 弱 #f85149 |
| 行头图标 | 盾牌 |
| 列头图标 | 骷髅 / 文件夹 / 终端 |

**D 类 — VIS-0.3-C 权限模型对照表（必出图 2）**

| 字段 | 值 |
|---|---|
| 标题 | 权限模型对照表 / 6 种权限模式 × 工具行为矩阵 |
| 6 种权限模式 (列) | **plan / default / acceptEdits / autoAccept / bypass / dontAsk** |
| 工具行 | Read / Glob / Grep / Edit / Write / Bash / Agent / MCP / NotebookEdit (至少 6 种) |
| 三色编码 | 绿=自动 / 黄=确认 / 红=禁止 |
| 状态图标 | ✓ / ⚠ / ✗ |
| **default 列必须高亮** | 蓝色边框 + "推荐"标签 |
| 脚注 | dontAsk 模式对需确认的操作直接拒绝（而非执行） |

**E 类 — 必出现的金句**

| # | 金句 | 来源 |
|---|------|------|
| Q1 | **「十步权限状态机、seatbelt/bwrap 进程隔离、bypass-immune 规则——多层防御纵深」** | L116 (从 OS 表行抽出) |
| Q2 | **「安全不是一个开关，而是矩阵化、可配置、有兜底的纵深体系」** | Opus 拟稿 (基于 Block 7a prep §二目标读者认知) |
| Q3 | **「default 是平衡点；想完全自动？看 acceptEdits/autoAccept；想完全自由？看 bypass，但要承担风险」** | Opus 拟稿 (基于子块叙事弧 §收尾) |

### 2.3 严禁出现的内容

| 内容 | 严禁原因 |
|------|---------|
| MCP / Hooks / Skills / 扩展机制 / 27 个 Hook 事件 / 8 种 MCP 传输 | Block 7b 弹药 |
| 配置层 / 模块依赖网络 / Tool.ts 入度 | Block 7c 弹药 |
| 内核 / 系统调用 / 三件事面板 / 43 工具目录 / 课程关联 | Block 6 弹药 |
| Block 1-5 / Block 8-9 任何元素 | 跨块边界 |
| Block 4 hero 数字 (1,884 / 476,875 / 40 / 98 / "操作系统" pull-quote) | 已用 |

## 3. 视觉语言

### 3.1 推荐视觉组件

| 组件 | 用途 | 优先级 |
|---|---|---|
| **prologue-marginal-label** + **hero-footer** (块号 07a + 块名 + sub-title) | 全书 spec | **P0** |
| **block-title** "安全防线" | H2 | **P0** |
| **os-table-anchor-card** (摘取的"安全模型 (Security)" 行) | 开头锚点 | **P0** |
| **insight-block 💡 小区门禁** | 通俗类比 | **P0** |
| **vis-0-3-b-matrix** (4×3 安全矩阵) | 第 1 张图 | **P0** |
| **q2-pull-quote** "安全不是一个开关" | 中段反思 | P1 |
| **vis-0-3-c-permission-table** (6 模式 × 工具行为) | 第 2 张图 | **P0** |
| **q3-balance-statement** "default 是平衡点" | 收束 | **P0** |
| **block-7b-bridge** | 块尾过渡 | **P0** |

### 3.2 节奏建议

1. **顶部 marginal label + hero footer**："序章 · Block 07a · 安全防线"
2. **块标题**："## 安全防线"
3. **OS 表锚点小卡片**：摘取"安全模型 (Security) → 权限系统 + 沙箱 + 企业策略"行 + 标签"OS 类比锚点"
4. **小区门禁通俗理解**：💡 卡片，逐字保留
5. **过渡句**：「但门禁体系不止有一道门——它是四层 × 三种威胁的矩阵」
6. **VIS-0.3-B 4×3 安全矩阵**：12 单元格 + 三色编码 + 行头盾牌 / 列头威胁图标
7. **过渡句 + Q2 pull-quote**："安全不是一个开关，而是矩阵化、可配置、有兜底的纵深体系。四层防御中，'权限系统'是最贴近用户的一层——而权限系统本身又有 6 种模式，每种模式下不同工具的行为不一样。"
8. **VIS-0.3-C 6×N 权限矩阵**：6 模式 × 工具行 + 三色 + ✓⚠✗ 图标 + default 蓝边框高亮
9. **Q3 收束**："default 是平衡点；想完全自动？看 acceptEdits/autoAccept；想完全自由？看 bypass，但要承担风险"
10. **块尾 Block 7b 过渡**：「但 Claude 不只有内置工具。它还能接入外部工具、被外部钩子拦截、被第三方扩展加新能力——这些'对外的延展面'是另一组完全不同的设计。」

### 3.3 字体/留白/对比度建议

- 块标题：`var(--cc-font-size-4xl)` ~ `5xl`，bold
- OS 表锚点小卡：背景 surface-secondary，"OS 类比锚点"标签 + 行内容
- VIS-0.3-B 矩阵：CSS Grid 12 格，三色填充 + hover 展开侧抽屉显示源码
- VIS-0.3-C 表格：HTML `<table>` + sticky 行头/列头 + default 列蓝边框
- Q2 / Q3 pull-quote：`var(--cc-font-size-2xl)` ~ `3xl`
- 块尾过渡：`var(--cc-font-size-xl)`
- block padding：`var(--cc-space-12)` 上下
- 视觉冲击力：信息密度高但不抢 Block 4 的情绪顶点

## 4. 双源理解（2 张图）

### 4.1 VIS-0.3-B 安全模型全景图

- **Brief 期望**：4×3 矩阵，行=防御层（左侧盾牌），列=威胁向量（顶部图标），三色编码，hover 展开
- **HTML 现状** (`test-viz/production/html/VIS-0-008_安全模型全景图.html` 或类似，948 行 / 7/10)：
  - 实现：矩阵核心结构 / 三色编码 / 背景六边形纹理 / 安全评分仪表盘
  - 缺：12 单元格"源码引用"未必落地，点击跳转可能缺失
- **V2 改造**：CSS Grid 12 格直接上墙（不 iframe），右侧加"整体安全评分"大仪表盘
- **必保留**：4×3 结构 + 三色编码 + 12 单元格不空 + 行头盾牌 + 列头图标

### 4.2 VIS-0.3-C 权限模型对照表

- **Brief 期望**：HTML table + 6 模式 × 工具行 + 三色 + default 高亮 + 排序
- **HTML 现状** (评分 9/10，实现最完整)：
  - 实现：6 模式列头 + 工具行 + 三色 + ✓⚠✗ 图标 + default 高亮 + 排序按钮 + detail-panel
- **V2 改造**：天然 compare-table，直接落地
- **必保留**：6×8+ 矩阵 + 三色 + ✓⚠✗ 图标 + default 蓝边框 + 脚注 dontAsk 拒绝规则

## 5. 评审 Checklist (满分 65)

| # | 检查项 | 评分依据 | 分值 |
|---|---|---|---|
| 1 | OS 表锚点小卡 (摘取"安全模型 (Security)" 行) | grep `安全模型` `Security` `权限系统 + 沙箱 + 企业策略` | 5 |
| 2 | 小区门禁 💡 通俗理解逐字 | grep `小区门禁` `业主` `访客` `外卖` `可疑人员` | 5 |
| 3 | VIS-0.3-B 4×3 安全矩阵存在 | grep `权限系统\|沙箱\|命令预检\|企业策略` (4 防御层) + `Prompt 注入\|文件越权\|命令逃逸` (3 威胁) | 5 |
| 4 | 12 单元格三色编码 | grep `#3fb950\|#d29922\|#f85149` 或对应 token | 5 |
| 5 | VIS-0.3-C 6 种权限模式全在 | grep `plan` `default` `acceptEdits` `autoAccept` `bypass` `dontAsk` | 5 |
| 6 | 工具行至少 6 种 | grep `Read` `Edit` `Bash` `Agent` 等 | 5 |
| 7 | default 列高亮 | grep `recommended\|default` 高亮样式 | 5 |
| 8 | dontAsk 拒绝脚注 | grep `dontAsk` `直接拒绝` | 5 |
| 9 | Q1 "十步权限状态机、seatbelt/bwrap 进程隔离" 逐字 | grep | 5 |
| 10 | Q2 "安全不是一个开关，而是矩阵化、可配置、有兜底的纵深体系" 逐字 | grep | 5 |
| 11 | Q3 "default 是平衡点" 收束 | grep | 5 |
| 12 | 块尾 Block 7b 过渡 | grep `对外的延展面\|外部工具\|另一组完全不同的设计` | 5 |
| 13 | 严禁红线: grep `MCP` `Hooks` `Skills` `配置层` `模块依赖` `1,884` `476,875` `操作系统` `内核` `Syscall` `43 个内置工具` `这不是使用指南` `让我们开始` `5 比喻家族` 全 0 | 任 1 命中 = -3 分 | 5 |

**满分 65 分**，**≥52 分**才允许进入下一阶段融合。

## 6. 输出契约

- 文件名：`v2-demos/block7a/prologue-block7a-<kimi|minimax>-<timestamp>.html`
- 文件大小预算：50-100 KB（含 2 张矩阵图）
- 行数预算：800-1500 行

## 7. 特殊提醒

- **本块灵魂 = 安全矩阵 + 权限对照表两张图**。如果两张图做成"列表"或"折叠卡"，整个块就废了。
- **default 列必须明显高亮**（OQ5）。这是读者快速决策的视觉锚点。
- **小区门禁通俗理解必须逐字保留**——它是 Block 7a 唯一一段降低非技术读者门槛的素材。
- **OS 表锚点小卡是新组件**（OQ1 选项 B）：摘取一行 OS 类比表，标"OS 类比锚点"，提醒读者"我们现在在哪一行"。Block 7b/7c 也会用同样的组件（设备驱动行 / 配置行）。
- **不要展开 Block 7b/7c 的内容**：哪怕只是隐晦提及"扩展" / "配置" 也要小心。

## 8. 与其他子 Agent 的协作约定

- 双跑模式：Kimi + MiniMax 独立产出
- 评审 sub-agent 用 §5 checklist 各打一份分
- 最终融合由 Opus 决定

---

*Brief by Opus on 2026-04-07 04:20 — 严格基于 Block 7 prep §二 + 序章 L116-122 逐字 + Opus 5 OQ 决断，零虚构。Block 7a 是 Block 7 信息密度顶点的第 1 拍，安全防线技术深潜。*
